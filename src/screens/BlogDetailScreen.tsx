import React, {Component} from 'react';
import {Text, StyleSheet, View} from 'react-native';

const BlogDetailScreen = () => {
  return (
    <View>
      <Text> BlogDetailScreen component </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BlogDetailScreen;
