import React, {Component} from 'react';
import {Text, StyleSheet, View} from 'react-native';

const BlogScreen = () => {
  return (
    <View>
      <Text> BlogScreen component </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default BlogScreen;
