- There are two files available in prototype code and screen shots
- Code file has complete user flow (Admin flow will be managed in beta phase due to condtional rendering)


Project Setup Steps:
- Unzip file
- install node modules  (npm install)
- To run the project (npx react-native start) and in separate terminal run command (npx react-native run-android)



Flow You can check the screens by click on login button to navgate home screen, and all navigation are working in app to see different pages


